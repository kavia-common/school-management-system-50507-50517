# PUBLIC_INTERFACE
# REACT_APP_API_BASE_URL sets the base URL for FastAPI backend.
# Ask the operator to set it in the environment or .env file for deployments.
# For local dev, if backend runs on 3001:
REACT_APP_API_BASE_URL=http://localhost:3001
